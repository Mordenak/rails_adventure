

<% if @user %>
  <%= @user.name %><br />
  <%= "used pw: #{@user.password}" %><br />
  <%= "recorded: #{@user.hashed_password}" %><br />
<% end %>

<%= link_to "Create character", action: 'new', controller: 'characters' %>

<% if !@user.characters.empty? %>
  <%= "stuff" %>
<% else %>
  "No characters"
<% end %>
